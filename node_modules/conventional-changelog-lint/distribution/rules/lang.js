'use strict';

Object.defineProperty(exports, "__esModule", {
	value: true
});

var _ensureLanguage2 = require('../library/ensure-language');

var _ensureLanguage3 = _interopRequireDefault(_ensureLanguage2);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = function (parsed, when, value) {
	var negated = when === 'never';

	var _ensureLanguage = (0, _ensureLanguage3.default)(parsed.subject, value),
	    matches = _ensureLanguage.matches,
	    detected = _ensureLanguage.detected;

	return [negated ? !matches : matches, ['commit', negated ? 'may not' : 'must', `be in languague "${value}", was one of: ${detected.join(', ')}`].filter(Boolean).join(' ')];
}; // TODO: this should be named subject-lang


module.exports = exports['default'];